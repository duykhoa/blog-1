<%= form_for([@article, @article.comments.build]) do |f| %>
  <% if @comment && @comment.errors.any? %>
    <div>
      <h2 class="error_explanation"><%= pluralize(@comment.errors.count, "error") %> prohibited this from being saved:</h2>
      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li class="error_explanations"><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="write_comment">
    <div class="field">
      <%= f.text_field :title, :placeholder => "Name", class: "text_input" %>
    </div>
    <div class="field">
      <%= f.text_area :body, :placeholder => "Comment", class: "text_input" %>
    </div>
    <div>
      <%= f.submit class: "submit" %>
    </div>
  </div>
<% end %>